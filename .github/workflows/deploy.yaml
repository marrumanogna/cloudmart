name: CloudMart Infrastructure Deployment

on:
  push:
    branches:
      - main
    paths:
      - 'cloudformation/**'
      - '.github/workflows/deploy.yaml'

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ap-south-1
  ENVIRONMENT: dev

jobs:

  # ============================================================
  # NETWORK
  # ============================================================

  deploy-network:
    name: Deploy Network Stack
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate Network Template
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/network-stack.yaml

      - name: Deploy Network Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/network-stack.yaml \
            --stack-name cloudmart-network-${{ env.ENVIRONMENT }} \
            --parameter-overrides \
              Environment=${{ env.ENVIRONMENT }} \
            --capabilities CAPABILITY_NAMED_IAM


  # ============================================================
  # DATA
  # ============================================================

  deploy-data:
    name: Deploy Data Stack
    needs: deploy-network
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate Data Template
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/data-stack.yaml

      - name: Deploy Data Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/data-stack.yaml \
            --stack-name cloudmart-data-${{ env.ENVIRONMENT }} \
            --parameter-overrides \
              Environment=${{ env.ENVIRONMENT }} \
              DBUsername="${{ secrets.DB_USERNAME }}" \
              DBPassword="${{ secrets.DB_PASSWORD }}" \
            --capabilities CAPABILITY_NAMED_IAM

      - name: Store Database Parameters in SSM
        env:
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

        run: |
          RDS_HOST=$(aws cloudformation describe-stacks \
            --stack-name cloudmart-data-${{ env.ENVIRONMENT }} \
            --query "Stacks[0].Outputs[?OutputKey=='RdsEndpoint'].OutputValue" \
            --output text)

          RDS_PORT=$(aws cloudformation describe-stacks \
            --stack-name cloudmart-data-${{ env.ENVIRONMENT }} \
            --query "Stacks[0].Outputs[?OutputKey=='RdsPort'].OutputValue" \
            --output text)

          DB_NAME=$(aws cloudformation describe-stacks \
            --stack-name cloudmart-data-${{ env.ENVIRONMENT }} \
            --query "Stacks[0].Outputs[?OutputKey=='DBName'].OutputValue" \
            --output text)

          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/rds/host" \
            --type "String" \
            --value "$RDS_HOST" \
            --overwrite

          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/rds/port" \
            --type "String" \
            --value "$RDS_PORT" \
            --overwrite

          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/rds/db-name" \
            --type "String" \
            --value "$DB_NAME" \
            --overwrite

          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/rds/username" \
            --type "SecureString" \
            --value "$DB_USERNAME" \
            --overwrite

          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/rds/password" \
            --type "SecureString" \
            --value "$DB_PASSWORD" \
            --overwrite


  # ============================================================
  # AUTH
  # ============================================================

  deploy-auth:
    name: Deploy Auth Stack
    needs: deploy-data
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify Auth Token Secret
        env:
          AUTH_TOKEN: ${{ secrets.CLOUDMART_AUTH_TOKEN }}

        run: |
          if [ -z "$AUTH_TOKEN" ]; then
            echo "ERROR: CLOUDMART_AUTH_TOKEN secret is missing"
            exit 1
          fi

          echo "CLOUDMART_AUTH_TOKEN secret found"

      - name: Store Auth Token in SSM
        env:
          AUTH_TOKEN: ${{ secrets.CLOUDMART_AUTH_TOKEN }}

        run: |
          aws ssm put-parameter \
            --name "/cloudmart/${ENVIRONMENT}/auth/api-token" \
            --type "SecureString" \
            --value "$AUTH_TOKEN" \
            --overwrite

      - name: Validate Auth Template
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/auth-stack.yaml

      - name: Deploy Auth Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/auth-stack.yaml \
            --stack-name cloudmart-auth-${{ env.ENVIRONMENT }} \
            --parameter-overrides \
              Environment=${{ env.ENVIRONMENT }} \
              AuthTokenParameterName="/cloudmart/${{ env.ENVIRONMENT }}/auth/api-token" \
            --capabilities CAPABILITY_NAMED_IAM


  # ============================================================
  # API / APPLICATION
  # ============================================================

  deploy-api:
    name: Deploy API and Application Stack
    needs: deploy-auth
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate API Template
        run: |
          aws cloudformation validate-template \
            --template-body file://cloudformation/api-stack.yaml

      - name: Package API SAM Template
        run: |
          aws cloudformation package \
            --template-file cloudformation/api-stack.yaml \
            --s3-bucket cloudmart-data-dev-artifactsbucket-sa0b9hgkhvza \
            --output-template-file cloudformation/api-stack-packaged.yaml \
            --region ${{ env.AWS_REGION }}

      - name: Deploy API Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/api-stack-packaged.yaml \
            --stack-name cloudmart-api-${{ env.ENVIRONMENT }} \
            --parameter-overrides \
              Environment=${{ env.ENVIRONMENT }} \
              LowStockEmail="marrumanogna@gmail.com" \
            --capabilities CAPABILITY_NAMED_IAM \
            --region ${{ env.AWS_REGION }}

      - name: Show API Outputs
        run: |
          aws cloudformation describe-stacks \
            --stack-name cloudmart-api-${{ env.ENVIRONMENT }} \
            --query "Stacks[0].Outputs" \
            --output table